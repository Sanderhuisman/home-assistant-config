recorder:
  db_url: !secret recorder_url
  purge_keep_days: 7
  include:
    domains:
      - sensor
      - binary_sensor
      - switch
      - light
      - media_player
      - device_tracker
      - person
      - vacuum
